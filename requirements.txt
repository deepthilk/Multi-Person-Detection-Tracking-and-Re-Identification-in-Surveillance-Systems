opencv-python>=4.6.0
torch>=1.13.0
torchvision>=0.14.0
torchreid==0.2.5
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.7
ultralytics>=8.0.0
numpy>=1.21.0
deep-sort-realtime>=1.3.1
Pillow>=9.0.0
scipy>=1.7.0
matplotlib>=3.5.0
PyYAML>=6.0
tqdm>=4.62.0
scikit-learn>=1.0.0
gdown>=4.7.0

# Face encoding: ArcFace (insightface) is the primary backend
# (FACE_ENCODER_BACKEND = "arcface" in reidentification/face_encoder.py).
# onnxruntime is the CPU runtime insightface uses to run its models.
insightface>=0.7.3
onnxruntime>=1.16.0

# Optional dlib fallback (used only if insightface/ArcFace is unavailable).
# dlib on Windows needs a pre-built wheel, and face_recognition needs dlib.
# Install order: pip install dlib-bin && pip install face_recognition --no-deps
# See: https://github.com/z-mahmud22/Dlib_Windows_Python3.x
# face_recognition>=1.3.0   # not directly pip-installable on Windows 3.13